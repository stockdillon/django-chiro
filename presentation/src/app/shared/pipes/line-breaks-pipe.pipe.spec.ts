import { LineBreaksPipePipe } from './line-breaks-pipe.pipe';

describe('LineBreaksPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LineBreaksPipePipe();
    expect(pipe).toBeTruthy();
  });
});
