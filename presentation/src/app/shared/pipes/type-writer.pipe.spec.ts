import { TypeWriterPipe } from './type-writer.pipe';

describe('TypeWriterPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeWriterPipe();
    expect(pipe).toBeTruthy();
  });
});
